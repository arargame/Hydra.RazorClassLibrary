@page "/checkbox-component-test"
@using Hydra.RazorClassLibrary.Components
@using Hydra.RazorClassLibrary.Utils

<TestElementDisplay HeaderTitle="Checkbox Test" Debugger="@Debugger">
    <CheckboxComponent Debugger="@Debugger"
                       IsTested="true"
                       IsDisabled="true"
                       IsVisible="true"/>
</TestElementDisplay>

@code {
    private ComponentDebugger Debugger = new();

    protected override void OnInitialized()
    {
        base.OnInitialized();
    }
}
